package com.linkapital.core.services.notification.contract.enums;

public enum EmailType {
    NORMAL,
    RATE,
    PROPOSAL,
    /*
     * Direct Message to all notifications.emails in config
     */
    ALERT
}
